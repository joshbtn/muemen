
/**
 * Module dependencies.
 */

var ejs = require('../lib/muemenDB');

module.exports = {
    'Blank Test': function(assert){
        var html = "";
        var str = "";
        assert.equal(html, str);
    }
};
